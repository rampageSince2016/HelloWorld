子路由器的设置
	主路由：
		dhcp模式, 其他没有什么特别的改变，其实也可以不用dhcp模式，使用dhcp模式可以让子路由设置少一点
	子路由：
		LAN口: 
			把dhcp模式关掉，指定其他网段的ip, 如192.168.2.1
		wlan口:
			如果主路由设置为dhcp模式，则这里不用做任何改变

	需要注意的地方:
		1. 如果设置好了子路由的Lan口，突然间连不上子路由了，这很正常，因为子路由的wlan口没有连上主路由的lan口，得不到ip所以访问不了子路由器
